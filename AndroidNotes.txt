If there is no activity android:label, application android:label it will be used.
values-<2 char nation> is the pattern to use to support other languages
Logo is used inside the app (in the app page header), icon is used out of the app (app list menu, android desktop, ecc).

Logo and icon free download site: http://www.flaticon.com/

Activity name vs Application name
Activity name is used in app page header and in all apps menu page
Activity name is used in opened app list
Activity name is used in desktop
Application name is apparently never used
