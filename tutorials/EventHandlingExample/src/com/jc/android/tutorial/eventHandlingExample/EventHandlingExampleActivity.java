package com.jc.android.tutorial.eventHandlingExample;

import android.app.Activity;
import android.os.Bundle;

public class EventHandlingExampleActivity extends Activity {
    /**
     * Called when the activity is first created.
     */
    @Override
    public void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.event_handling_example_activity);
    }
}
